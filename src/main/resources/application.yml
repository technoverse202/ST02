
quarkus:
   datasource:
      db-kind:  
      jdbc:
        driver: 
        url:  
      username: 
      password: 
# drop and create the database at startup (use `update` to only update the schema)
   hibernate-orm:
      database:
         generation: update
